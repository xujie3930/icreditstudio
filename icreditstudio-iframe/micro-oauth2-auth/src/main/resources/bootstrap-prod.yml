spring:
  application:
    name: micro-oauth2-auth
  cloud:
    nacos:
      config:
        server-addr: 192.168.211.3:8848 #配置中心地址
        file-extension: yml #配置文件结尾的配置
        group: PROD
  profiles:
    active: prod