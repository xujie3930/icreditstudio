spring:
  application:
    name: micro-processengine
  cloud:
    nacos:
      config:
        server-addr: 192.168.0.107:8848 #配置中心地址
        file-extension: yml #配置文件结尾的配置
        group: RELEASE-GONGJIAN
  profiles:
    active: releasegongjian

# 日志文件配置
logging:
  file:
    name: ../logs/micro-processengine.log # 日志文件名，全路径


project:
  #参建单位列表接口
  queryPcListUrl: http://81.68.236.204:8288/pj-manage/v1/pic/queryPcList