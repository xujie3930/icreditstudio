spring:
  application:
    name: micro-oauth2-api
  cloud:
    nacos:
      config:
        server-addr: 192.168.0.234:8848 #配置中心地址
        file-extension: yml #配置文件结尾的配置
        group: RELEASE
  profiles:
    active: release

# 日志文件配置
logging:
  file:
    name: ../logs/micro-oauth2-api.log # 日志文件名，全路径